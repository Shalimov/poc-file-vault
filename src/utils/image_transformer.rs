use image;

pub fn tranform() -> String {
  image::ImageBuffer::from_vec(width: u32, height: u32, buf: Vec<P::Subpixel>)
  image::imageops::resize(image: &I, nwidth: u32, nheight: u32, filter: FilterType)
  String::from("Hello World")
}
